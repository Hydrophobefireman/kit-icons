import { css } from "catom";

export const buttonStyle = css({
  border: "2px solid",
  justifyContent: "center",
  display: "flex",
  //   margin: ".5rem",
  padding: "0.5rem",
});

export const innerButtonStyle = css({
  flexDirection: "column",
  alignItems: "center",
  justifyContent: "center",
});
